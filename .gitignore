--!strict
--[[
    Slender Hub - UNIVERSAL LOADER (Architect Edition V2)
    UI Library: SlenderWind (Fixed Logic)
    System: VeLink API Integration + Auto Game Detection
]]

--//==============================[ SERVICES ]==============================//--
local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local StarterGui = game:GetService("StarterGui")
local MarketService = game:GetService("MarketplaceService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local CurrentPlaceId = game.PlaceId

--//==============================[ ⚠ CONFIGURAÇÃO ]==============================//--
local VELINK_IDS = {
    ["ed6453e6-5259-49b4-8779-05b866831dc2"] = {Type = "Premium", Duration = 604800}, -- 7 Dias
    ["2c69fdcf-f8e1-4c1a-9270-f36d9545ebce"] = {Type = "Premium", Duration = 2592000} -- 30 Dias
}

local Config = {
    FOLDER = "SlenderHub_Universal", 
    FILE = "KeyData_" .. LocalPlayer.UserId .. ".json",
    VELINK_CHECK_URL = "https://velink.to/api/public/check/key",
    FREE_KEY_LINK = "https://velink.to/u/678807",
    PREMIUM_SHOP_URL = "https://www.slenderhub.shop/"
}

--//==============================[ JOGOS SUPORTADOS ]==============================//--
local GAMES_MAP = {
    [93787311916283] = "https://raw.githubusercontent.com/SlenderYt567/sc/refs/heads/main/Horse%20Race",
    [82013336390273] = "https://raw.githubusercontent.com/SlenderYt567/sc/main/Pickaxe%20Simulator%20script",
    [107807115409153] = "https://raw.githubusercontent.com/SlenderYt567/sc/main/Bee%20Tappers%20Script",
    [109983668079237] = "https://raw.githubusercontent.com/SlenderYt567/sc/main/Steal%20a%20Brainrot(Free)",
    [76558904092080] = "https://raw.githubusercontent.com/SlenderYt567/sc/main/The%20Forge%20Script",
    [75992362647444] = "https://raw.githubusercontent.com/SlenderYt567/sc/main/Tap%20simulator",
    [4924922222] = "https://raw.githubusercontent.com/SlenderYt567/sc/refs/heads/main/Brookhaven%20script"
}

local TARGET_SCRIPT_URL = GAMES_MAP[CurrentPlaceId]

--//==============================[ CHAVES LIFETIME ]==============================//--
local LIFETIME_KEYS = {
    ["LifeTimeF4K9L2P7V8JDHUtY"] = true,
    ["SlenderDevKey"] = true
}

local FILE_PATH = Config.FOLDER .. "/" .. Config.FILE
local isAccessAuthorized = false
local authorizedKeyType = "Free"
local authorizedDuration = 86400
local tempKeyInput = "" -- Armazena a key digitada temporariamente

--//==============================[ LÓGICA DE VALIDAÇÃO ]==============================//--
local function verifyKeyWithAPI(inputKey)
    print("[SlenderHub] Verifying Key:", inputKey)
    if LIFETIME_KEYS[inputKey] then return "Premium", math.huge end

    local requestUrl = string.format("%s?key=%s", Config.VELINK_CHECK_URL, inputKey)
    local requestFunc = (syn and syn.request) or (http and http.request) or (http_request) or (request)
    
    if not requestFunc then 
        warn("[SlenderHub] Executor not supported for HTTP Requests")
        return nil, nil 
    end

    local response = requestFunc({Url = requestUrl, Method = "GET"})

    if response and response.StatusCode == 200 then
        local success, data = pcall(function() return HttpService:JSONDecode(response.Body) end)
        if success and data and data.expired == false then
            local unlockerId = tostring(data.unlockerId)
            local tierInfo = VELINK_IDS[unlockerId]
            if tierInfo then return tierInfo.Type, tierInfo.Duration else return "Free", 86400 end
        else
            warn("[SlenderHub] API Error or Expired:", response.Body)
        end
    else
        warn("[SlenderHub] HTTP Failed:", response.StatusCode)
    end
    return nil, nil
end

local function formatTime(seconds)
    if seconds == math.huge then return "Lifetime" end
    local days = math.floor(seconds / 86400)
    local hours = math.floor((seconds % 86400) / 3600)
    local minutes = math.floor((seconds % 3600) / 60)
    return string.format("%dd %02dh %02dm", days, hours, minutes)
end

local function loadGameScript(accessLevel)
    if not TARGET_SCRIPT_URL then 
        warn("🔴 [Slender Hub] Script URL not found for Place ID: " .. CurrentPlaceId)
        return 
    end
    
    if getgenv then getgenv().SlenderHubAccessLevel = accessLevel else _G.SlenderHubAccessLevel = accessLevel end
    
    task.spawn(function() 
        local success, err = pcall(function() loadstring(game:HttpGet(TARGET_SCRIPT_URL))() end)
        if not success then warn("Failed to load script: " .. tostring(err)) end
    end)
end

local function saveKey(keyType, keyContent, duration)
    if not isfolder(Config.FOLDER) then makefolder(Config.FOLDER) end
    local data = {Type = keyType, Key = keyContent, Time = os.time(), CustomDuration = duration}
    writefile(FILE_PATH, HttpService:JSONEncode(data))
end

local function checkLocalKey()
    if not isfile(FILE_PATH) then return false, nil end
    local success, data = pcall(function() return HttpService:JSONDecode(readfile(FILE_PATH)) end)
    if not success or not data or not data.Time then delfile(FILE_PATH); return false, nil end

    local duration = data.CustomDuration or 86400
    if duration == math.huge then return true, data, math.huge end

    local elapsed = os.time() - data.Time
    if elapsed > duration then delfile(FILE_PATH); return false, nil end
    return true, data, (duration - elapsed)
end

--//==============================[ AUTO-EXECUTE ]==============================//--
local isSaved, savedData, remainingTime = checkLocalKey()

if isSaved and TARGET_SCRIPT_URL then
    local timeMsg = (remainingTime == math.huge) and "Lifetime Access" or formatTime(remainingTime)
    StarterGui:SetCore("SendNotification", {Title = "Slender Hub", Text = "Auto-Login! Remaining: " .. timeMsg, Duration = 8})
    loadGameScript(savedData.Type)
    return 
end

--//==============================[ UI SLENDERWIND ]==============================//--
-- Certifique-se de que o link abaixo contém a versão V7 ou V6 funcional
local Library = loadstring(game:HttpGet("https://raw.githubusercontent.com/SlenderYt567/sc/refs/heads/main/SlenderWindV5.lua"))()

-- Tema Personalizado (Opcional, funciona se você atualizou para a V7)
local MyTheme = {
    Accent = Color3.fromRGB(0, 255, 128), -- Verde Slender
    Sidebar = Color3.fromRGB(20, 20, 20)
}

local Window = Library.Window({
    Name = "Slender Hub | Gateway",
    Keybind = Enum.KeyCode.RightControl,
    KeySystem = false,
    Theme = MyTheme
})

Window:Watermark({ Enabled = true, Title = "SlenderHub | Security Gateway" })

local Tab = Window:Tab("Access", "rbxassetid://3926305904")

-- Info do Jogo
local gameName = "Unknown Game"
pcall(function() gameName = MarketService:GetProductInfo(CurrentPlaceId).Name end)
local statusText = TARGET_SCRIPT_URL and "✅ Supported: " .. gameName or "⚠ Unsupported Game"

Tab:Section("System Status")
Tab:Paragraph("Game Info", statusText .. "\n(ID: " .. CurrentPlaceId .. ")")

-- Input da Key
Tab:Section("Authentication")

Tab:Input("Enter Key", "Paste VELINK-XXXX...", function(input)
    tempKeyInput = input:gsub("%s+", "") -- Salva a key numa variável temporária
end)

-- Botão de Verificar Separado (Mais seguro que depender do Enter)
Tab:Button("🔎 Verify Key", function()
    if tempKeyInput == "" then
        Window:Notify("Error", "Please paste a key first!", 3)
        return
    end

    Window:Notify("System", "Verifying Tier & Duration...", 2)
    
    local keyType, keyDuration = verifyKeyWithAPI(tempKeyInput)
    
    if keyType and keyDuration then
        isAccessAuthorized = true
        authorizedKeyType = keyType
        authorizedDuration = keyDuration
        saveKey(keyType, tempKeyInput, keyDuration)
        
        local durText = (keyDuration == math.huge) and "Lifetime" or formatTime(keyDuration)
        Window:Notify("Success", keyType .. " Key Validated (" .. durText .. ")", 5)
    else
        isAccessAuthorized = false
        Window:Notify("Error", "Invalid or Expired Key", 3)
    end
end)

-- Botão de Launch
Tab:Button("🚀 Launch Hub", function()
    if not isAccessAuthorized then
        Window:Notify("Access Denied", "Please verify a valid key first.", 3)
        return
    end
    
    if not TARGET_SCRIPT_URL then
         Window:Notify("Error", "Script not available for this game.", 3)
         return
    end
    
    local isSaved, data, remaining = checkLocalKey()
    local timeMsg = (remaining == math.huge) and "Lifetime" or formatTime(remaining)
    
    Window:Notify("Access Granted", "Loading Script... (" .. timeMsg .. ")", 5)

    -- Destruir UI e Carregar Script
    task.delay(1, function()
        if CoreGui:FindFirstChild("SlenderWind") then
            CoreGui.SlenderWind:Destroy()
        end
    end)
    
    loadGameScript(authorizedKeyType)
end)

-- Links
Tab:Section("Get Access")

Tab:Button("🔑 Get Free Key (24h)", function()
    setclipboard(Config.FREE_KEY_LINK)
    Window:Notify("Copied", "Link copied to clipboard!", 2)
end)

Tab:Button("💎 Buy Premium (Shop)", function()
    setclipboard(Config.PREMIUM_SHOP_URL)
    Window:Notify("Shop Copied", "Visit SlenderHub.shop", 3)
end)
